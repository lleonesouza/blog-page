import * as React from "react";
import { ModalDiv0 } from '../../../styled-tags'

const makeContact = () => {
  return () => {
    return (
      <ModalDiv0>
        <h1>lleonesouza@live.com</h1>
      </ModalDiv0>
    );
  };
};

export default makeContact
