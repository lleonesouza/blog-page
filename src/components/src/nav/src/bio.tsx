import * as React from "react";
import { Img } from "../../../../styled-tags";

const makeBio = () => {
  return () => {
    return (
      <figure>
        <Img src="" alt="lleonesouza" />
      </figure>
    );
  };
};

export default makeBio;
